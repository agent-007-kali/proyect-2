# 🎨 Visual System Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                         🌍 USER LAYER                                │
└─────────────────────────────────────────────────────────────────────┘

                                  👤 User
                                   │
                    ┌──────────────┼──────────────┐
                    │              │              │
                    ↓              ↓              ↓
            ┌──────────┐   ┌──────────┐   ┌──────────┐
            │ Dashboard│   │   URL    │   │  Email   │
            │  Viewer  │   │ Manager  │   │  Inbox   │
            └──────────┘   └──────────┘   └──────────┘
                    │              │              ↑
                    └──────────────┼──────────────┘
                                   │
┌─────────────────────────────────────────────────────────────────────┐
│                    🌐 FRONTEND LAYER (Vercel)                        │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │                    Next.js Application                       │   │
│  │                                                              │   │
│  │  Components:               API Routes:                      │   │
│  │  • Dashboard.tsx           • /api/get-report                │   │
│  │  • URLManager.tsx          • /api/update-targets            │   │
│  │                                                              │   │
│  │  Libraries:                                                  │   │
│  │  • @supabase/supabase-js                                    │   │
│  │  • Tailwind CSS                                             │   │
│  │  • Lucide Icons                                             │   │
│  └────────────────────────────────────────────────────────────┘   │
│                                   │                                 │
└───────────────────────────────────┼─────────────────────────────────┘
                                    │
                    HTTPS REST API  │  HTTPS REST API
                                    │
┌───────────────────────────────────┼─────────────────────────────────┐
│                    💾 DATABASE LAYER (Supabase)                      │
│                                   │                                 │
│  ┌────────────────────────────────┴────────────────────────────┐  │
│  │                  PostgreSQL Database                         │  │
│  │                                                              │  │
│  │  ┌────────────────────┐      ┌──────────────────────────┐  │  │
│  │  │  subscriptions     │      │  monitoring_jobs         │  │  │
│  │  ├────────────────────┤      ├──────────────────────────┤  │  │
│  │  │ • user_email (PK)  │←────┤│ • id (PK)                │  │  │
│  │  │ • status           │ FK  ││ • user_email (FK)        │  │  │
│  │  │ • plan             │      ││ • url_1, url_2, url_3   │  │  │
│  │  │ • created_at       │      ││ • last_check            │  │  │
│  │  │ • updated_at       │      ││ • report_history (JSONB)│  │  │
│  │  └────────────────────┘      │└──────────────────────────┘  │  │
│  │                                                              │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                   │                                 │
└───────────────────────────────────┼─────────────────────────────────┘
                                    │
                    Python SDK     │    Python SDK
                                    │
┌───────────────────────────────────┼─────────────────────────────────┐
│                 🤖 INTELLIGENCE LAYER (Kali Linux)                   │
│                                   │                                 │
│  ┌────────────────────────────────┴────────────────────────────┐  │
│  │                    orchestrator.py                           │  │
│  │                                                              │  │
│  │  Every hour:                                                 │  │
│  │  1. Query Supabase for active subscriptions                 │  │
│  │  2. Find jobs older than 24 hours                           │  │
│  │  3. For each job:                                            │  │
│  │     └─→ Call run_intel_cycle(user, urls)                    │  │
│  └────────────────────────────┬─────────────────────────────────┘  │
│                                │                                    │
│                                ↓ Function Call                      │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                        agent.py                             │  │
│  │                                                             │  │
│  │  run_intel_cycle():                                         │  │
│  │  ┌──────────────────────────────────────────────────────┐  │  │
│  │  │ 1. Scrape URLs                                       │  │  │
│  │  │    └─→ scrape_url() × 3                             │  │  │
│  │  │                                                       │  │  │
│  │  │ 2. Get previous report from Supabase                │  │  │
│  │  │                                                       │  │  │
│  │  │ 3. Compare old vs new                                │  │  │
│  │  │    └─→ If changes → Continue                        │  │  │
│  │  │                                                       │  │  │
│  │  │ 4. Ask AI for analysis                               │  │  │
│  │  │    └─→ ask_local_ai(prompt) → Ollama               │  │  │
│  │  │                                                       │  │  │
│  │  │ 5. Save report to Supabase                          │  │  │
│  │  │    └─→ Update report_history (JSONB)               │  │  │
│  │  │                                                       │  │  │
│  │  │ 6. Send email alert                                  │  │  │
│  │  │    └─→ send_email_alert() → Gmail SMTP             │  │  │
│  │  └──────────────────────────────────────────────────────┘  │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                │                                   │
│                                ↓ HTTP POST                         │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │              Ollama (localhost:11434)                      │  │
│  │                                                             │  │
│  │  Model: llama3.2:3b                                        │  │
│  │  Task: Competitive intelligence analysis                    │  │
│  └────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagrams

### 1️⃣ User Configures Competitor URLs

```
   👤 User
    │
    ↓ Enters URLs
┌──────────────────┐
│ URLManager.tsx   │
│ (Frontend)       │
└────────┬─────────┘
         │
         ↓ POST /api/update-targets
         │ Body: { user_email, url_1, url_2, url_3 }
         │
┌────────┴─────────┐
│ Next.js API Route│
│ route.ts         │
└────────┬─────────┘
         │
         ↓ 1. Validate user has active subscription
         ↓ 2. Validate URL formats
         ↓ 3. Upsert monitoring_jobs record
         │
┌────────┴─────────┐
│ Supabase DB      │
│ monitoring_jobs  │
└──────────────────┘
         │
         ↓ Success response
         │
   👤 User sees: "✅ URLs updated successfully"
```

### 2️⃣ Orchestrator Processes Jobs (Every Hour)

```
⏰ Cron-like Loop (every 3600s)
         │
         ↓
┌────────────────────────────┐
│ orchestrator.py            │
│ get_jobs_to_process()      │
└────────┬───────────────────┘
         │
         ↓ SQL Query
         │ SELECT * FROM monitoring_jobs
         │ WHERE subscription.status = 'active'
         │ AND (last_check IS NULL OR last_check < NOW() - 24h)
         │
┌────────┴──────────┐
│ Supabase DB       │
└────────┬──────────┘
         │
         ↓ Returns: [job1, job2, job3, ...]
         │
┌────────┴───────────────────┐
│ For each job:              │
│   process_job(job)         │
│   ↓                        │
│   run_intel_cycle(         │
│     user_email,            │
│     [url_1, url_2, url_3], │
│     supabase_client,       │
│     report_history         │
│   )                        │
└────────────────────────────┘
```

### 3️⃣ Agent Processes Single User

```
┌──────────────────────────────┐
│ run_intel_cycle()            │
│ (user: john@example.com)     │
└────────┬─────────────────────┘
         │
         ↓ Scrape URLs in parallel
         │
    ┌────┴────┬─────────┐
    │         │         │
    ↓         ↓         ↓
┌───────┐ ┌───────┐ ┌───────┐
│ URL 1 │ │ URL 2 │ │ URL 3 │
└───┬───┘ └───┬───┘ └───┬───┘
    │         │         │
    └─────┬───┴───┬─────┘
          ↓       ↓
    Current HTML Content
          │
          ↓ Get from Supabase
          │
    Previous HTML Content
          │
          ↓ Compare
          │
    Changes Detected? ────No────→ Update last_check → End
          │
          Yes
          ↓
    ┌─────────────────┐
    │ ask_local_ai()  │
    │                 │
    │ Prompt:         │
    │ "Compare old    │
    │  vs new and     │
    │  analyze..."    │
    └────────┬────────┘
             │
             ↓ HTTP POST
    ┌────────────────────┐
    │ Ollama             │
    │ llama3.2:3b        │
    └────────┬───────────┘
             │
             ↓ AI Analysis
    "Competitor launched new
     pricing page at $99/mo..."
             │
             ↓
    ┌──────────────────────────┐
    │ Save to Supabase:        │
    │                          │
    │ report_history.append({  │
    │   timestamp: now,        │
    │   urls: [...],          │
    │   analysis: "...",      │
    │   changes_detected: true │
    │ })                       │
    └────────┬─────────────────┘
             │
             ↓
    ┌──────────────────────────┐
    │ send_email_alert()       │
    │                          │
    │ To: john@example.com     │
    │ Subject: 🕵️ Intel Alert │
    │ Body: [AI Report]        │
    └────────┬─────────────────┘
             │
             ↓ SMTP
    ┌──────────────────┐
    │ Gmail            │
    └────────┬─────────┘
             │
             ↓
        📧 Email Delivered
```

### 4️⃣ User Views Dashboard

```
   👤 User visits dashboard
    │
    ↓ Component mounts
┌──────────────────┐
│ Dashboard.tsx    │
│ useEffect()      │
└────────┬─────────┘
         │
         ↓ GET /api/get-report?user_email=john@example.com
         │
┌────────┴─────────┐
│ Next.js API Route│
│ route.ts         │
└────────┬─────────┘
         │
         ↓ SELECT report_history, last_check, urls
         │ FROM monitoring_jobs
         │ WHERE user_email = 'john@example.com'
         │
┌────────┴──────────┐
│ Supabase DB       │
└────────┬──────────┘
         │
         ↓ Returns JSONB data
         │
┌────────┴─────────┐
│ {                │
│   latest_report: │
│   {              │
│     timestamp,   │
│     urls,        │
│     analysis,    │
│     changes_det  │
│   },             │
│   total_reports, │
│   last_check,    │
│   monitored_urls,│
│   report_history │
│ }                │
└────────┬─────────┘
         │
         ↓ JSON Response
         │
┌────────┴──────────┐
│ Dashboard.tsx     │
│ setData(result)   │
└────────┬──────────┘
         │
         ↓ Render UI
         │
   👤 User sees:
   ┌─────────────────────────────┐
   │ 📊 Total Reports: 5         │
   │ ⏰ Last Check: 2 hours ago  │
   │ 🔍 Monitoring: 3 URLs       │
   │                             │
   │ 🕵️ Latest Intelligence:    │
   │ [AI Analysis displayed]     │
   └─────────────────────────────┘
```

---

## Component Interaction Map

```
┌─────────────────────────────────────────────────────────────┐
│                     FRONTEND COMPONENTS                      │
│                                                              │
│  ┌─────────────┐                    ┌─────────────┐        │
│  │ URLManager  │                    │  Dashboard  │        │
│  │             │                    │             │        │
│  │ • 3 inputs  │                    │ • Stats     │        │
│  │ • Validate  │                    │ • Latest    │        │
│  │ • Submit    │                    │ • History   │        │
│  └──────┬──────┘                    └──────┬──────┘        │
│         │                                  │               │
│         │ POST                             │ GET           │
│         │                                  │               │
└─────────┼──────────────────────────────────┼───────────────┘
          │                                  │
          ↓                                  ↓
┌─────────────────────────────────────────────────────────────┐
│                        API ROUTES                            │
│                                                              │
│  ┌──────────────────────┐      ┌──────────────────────┐   │
│  │ /api/update-targets  │      │  /api/get-report     │   │
│  │                      │      │                      │   │
│  │ • Validate user      │      │ • Query reports      │   │
│  │ • Validate URLs      │      │ • Format response    │   │
│  │ • Upsert DB          │      │ • Return JSON        │   │
│  └───────────┬──────────┘      └──────────┬───────────┘   │
│              │                            │               │
└──────────────┼────────────────────────────┼───────────────┘
               │                            │
               ↓                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    SHARED LAYER                              │
│                                                              │
│                ┌──────────────────┐                         │
│                │ src/lib/         │                         │
│                │ supabase.ts      │                         │
│                │                  │                         │
│                │ createClient()   │                         │
│                └────────┬─────────┘                         │
│                         │                                   │
└─────────────────────────┼───────────────────────────────────┘
                          │
                          ↓
┌─────────────────────────────────────────────────────────────┐
│                      SUPABASE                                │
│                                                              │
│  Tables: subscriptions, monitoring_jobs                      │
│  Auth: service_role key                                      │
│  RPC: SQL queries                                            │
└─────────────────────────────────────────────────────────────┘
```

---

## File Dependency Tree

```
intel_agent/
├── orchestrator.py ─────┐
│   └── imports: agent   │
│                        │
├── agent.py ────────────┤
│   ├── requests         │
│   ├── beautifulsoup4   │
│   ├── supabase ────────┼─→ Talks to Supabase
│   ├── dotenv           │
│   └── smtplib          │
│                        │
├── .env ────────────────┼─→ Configuration
│                        │
├── requirements.txt     │
├── start-orchestrator.sh│
└── stop-orchestrator.sh │

proyects-2/
├── src/
│   ├── app/
│   │   └── api/
│   │       ├── update-targets/
│   │       │   └── route.ts ─────┐
│   │       │       └── imports    │
│   │       │           supabase ──┼─→ Talks to Supabase
│   │       │                      │
│   │       └── get-report/        │
│   │           └── route.ts ──────┤
│   │               └── imports    │
│   │                   supabase ──┤
│   │                              │
│   ├── components/               │
│   │   ├── Dashboard.tsx ────────┼─→ Fetches from API
│   │   │   └── calls:            │
│   │   │       /api/get-report   │
│   │   │                         │
│   │   └── URLManager.tsx ───────┼─→ Posts to API
│   │       └── calls:            │
│   │           /api/update-targets
│   │                             │
│   └── lib/                      │
│       └── supabase.ts ───────────┘
│           └── createClient()
│
└── .env.local ────────────────────→ Configuration
```

---

## Timing Diagram (24-hour cycle)

```
Time: 00:00 ────────────────────────────────────────────────────────→

User Action:
00:05  │ User updates URLs via URLManager
       │ └→ Saved to Supabase
       │

Orchestrator:
01:00  │ Cycle #1: Check database
       │ └→ Job found for user@example.com (never checked)
       │ └→ Process: Scrape, AI analysis, Email sent
       │
02:00  │ Cycle #2: Check database
       │ └→ No stale jobs (last_check = 01:00, only 1h old)
       │
03:00  │ Cycle #3: Check database
       │ └→ No stale jobs
       │
...    │ (repeat every hour)
       │
24:00  │ Cycle #24: Check database
       │ └→ No stale jobs
       │
25:00  │ Cycle #25: Check database (next day)
       │ └→ Job found! (last_check = 01:00, 24h ago)
       │ └→ Process: Scrape, detect changes, AI, Email
       │

Email Alerts:
01:05  │ 📧 Initial report sent
       │
25:05  │ 📧 Daily update sent (changes detected)
       │

Dashboard Views:
02:30  │ User views dashboard
       │ └→ Sees report from 01:00
       │
14:15  │ User refreshes dashboard
       │ └→ Still shows report from 01:00
       │
25:10  │ User views dashboard
       │ └→ Sees new report from 25:00
```

---

## Security Flow

```
🔐 SENSITIVE DATA FLOW

┌──────────────────────────────────────────────────────────┐
│                    Environment Variables                  │
│                                                           │
│  Backend (.env):                                         │
│  • SUPABASE_URL                                          │
│  • SUPABASE_SERVICE_ROLE_KEY ← Never exposed            │
│  • EMAIL_USER                                            │
│  • EMAIL_PASSWORD                                        │
│                                                           │
│  Frontend (.env.local):                                  │
│  • NEXT_PUBLIC_SUPABASE_URL ← OK to expose             │
│  • SUPABASE_SERVICE_ROLE_KEY ← Server-side only         │
│                                                           │
│  .gitignore prevents commit ✓                            │
└──────────────────────────────────────────────────────────┘
            │                            │
            ↓ Loaded at runtime          ↓
┌─────────────────────┐      ┌─────────────────────┐
│  Python (Backend)   │      │ Next.js (Frontend)  │
│                     │      │                     │
│ • All access OK     │      │ • API routes: OK    │
│ • Direct DB access  │      │ • Components: NO    │
└─────────────────────┘      └─────────────────────┘
            │                            │
            ↓                            ↓
┌──────────────────────────────────────────────────────────┐
│                      Supabase                             │
│                                                           │
│  Row Level Security (RLS):                               │
│  • Not yet configured (optional future feature)          │
│  • Currently using service_role key (bypasses RLS)       │
│                                                           │
│  Security relies on:                                      │
│  • API route validation                                  │
│  • Environment variable protection                        │
└──────────────────────────────────────────────────────────┘
```

---

## Deployment Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                         PRODUCTION                           │
└─────────────────────────────────────────────────────────────┘

┌─────────────────┐         ┌─────────────────┐
│   Vercel Edge   │         │  Kali Linux VM  │
│   (Frontend)    │         │   (Backend)     │
│                 │         │                 │
│ • Next.js       │         │ • Python 3.13   │
│ • API Routes    │         │ • Orchestrator  │
│ • Static UI     │         │ • Ollama        │
│ • Auto-scaling  │         │ • Systemd       │
└────────┬────────┘         └────────┬────────┘
         │                           │
         │ HTTPS                     │ Python SDK
         │                           │
         └───────────┬───────────────┘
                     ↓
         ┌───────────────────────┐
         │   Supabase Cloud      │
         │   (Database)          │
         │                       │
         │ • PostgreSQL          │
         │ • Auto-backups        │
         │ • Connection pooling  │
         └───────────────────────┘

Monitoring:
• Vercel Dashboard → Frontend logs
• journalctl → Backend logs
• Supabase Dashboard → DB metrics
```

---

**End of Visual Architecture Guide**



